PORT=3001
MONGODB_URI=mongodb://localhost:27017/ballu
JWT_SECRET=your-super-secret-key-change-in-production

# Stripe (checkout + webhooks)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
# Public site URL (success/cancel redirects). Use your real frontend URL in production.
FRONTEND_URL=http://localhost:5173

# Firebase Admin — pick ONE:
# Local: path to the JSON key file
GOOGLE_APPLICATION_CREDENTIALS=./service-account.json
# Hosting (Render, Railway, etc.): paste the entire JSON as one line (or use dashboard multiline)
# FIREBASE_SERVICE_ACCOUNT_JSON={"type":"service_account",...}
