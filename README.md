# Ball U Academy – React (Vite) frontend + MongoDB API

React store frontend with an optional **Node/Express + MongoDB** backend. Customer sign-up, orders, visit stats, and admin dashboard data are stored in MongoDB when the API is running.
